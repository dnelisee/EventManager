package com.polytechnique.finaltppoo2.service;

import com.polytechnique.finaltppoo2.model.Conference;

import javafx.collections.FXCollections;
import javafx.collections.ObservableList;

/*
 * This class implements the business logic related to Conference 
*/
public class ConferenceService {
    /*
    private ConferenceDao dao; 
    private ObservableList<Conference> confObsrvList = FXCollections.observableArrayList();
    
    public ConferenceService(ConferenceDao dao) {
        this.dao = dao; 
        confObsrvList.addAll(dao.getConfList()); 
    }

    public ObservableList<Conference> getConfObsrvList() {
        return confObsrvList; 
    }

    public void addConference(Conference conf) {
        dao.addConference(conf);
        confObsrvList.add(conf); 
    } 

    public void removeConference(Conference conf) {
        dao.removeConference(conf); 
        confObsrvList.remove(conf); 
    }
    */

}
